export enum AvailabilityType {
  ALWAYS_OPEN = 'ALWAYS_OPEN',
  TEMPORARILY_CLOSED = 'TEMPORARILY_CLOSED',
  SELECTED_HOURS = 'SELECTED_HOURS',
}

export enum TimeSlotUnit {
  MINUTES = 'Minutes',
  HOURS = 'Hours',
  DAYS = 'Days',
}
